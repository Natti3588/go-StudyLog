# StudyLog Frontend

StudyLog の学習記録アプリのフロントエンドです。

## セットアップ

```bash
pnpm install
```

## 主なコマンド

```bash
pnpm dev       # 開発サーバーを起動
pnpm build     # 本番用にビルド
pnpm test      # テストを実行
pnpm lint      # Lint を実行
pnpm preview   # ビルド結果をプレビュー
```
